// Copy to wifi_config.h and fill in local values before flashing ESP32.
// wifi_config.h is ignored by Git because it can contain Wi-Fi credentials.

#pragma once

static const char WIFI_SSID[] = "YOUR_WIFI_SSID";
static const char WIFI_PASSWORD[] = "YOUR_WIFI_PASSWORD";
static const char PC_HOST[] = "172.20.10.2";
